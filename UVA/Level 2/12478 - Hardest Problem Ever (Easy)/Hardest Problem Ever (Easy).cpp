#include <bits/stdc++.h>
#define INF INT_MAX
#define MINF INT_MIN
#define ll long long
#define PB push_back
#define PF push_front
#define P_F pop_front
#define P_B pop_back
#define F front
#define B back
#define MP make_pair
#define FOR(i, a, b) for(int i = a; i < b; i++)
#define forit(i, s) for(__typeof ((s).end ()) i = (s).begin (); i != (s).end (); ++i)
#define foritr(i, s) for(__typeof ((s).rend ()) i = (s).rbegin (); i != (s).rend (); ++i)
 
using namespace std;

int main(){
  /*vector<string> v{ "OBIDAIBKR", "RKAULHISP", "SADIYANNO", "HEISAWHIA",
                    "IRAKIBULS", "MFBINTRNO", "LEBSYNUNE", "EMOTIONAL"};
                    
  vector<string> names{"RAKIBUL", "ANINDYA", "MOSHIUR", "SHIPLU", "KABIR", "SUNNY", "OBAIDA", "WASI"};
  vector<int> veces(8);
  int aux;
  string comp;
  
  FOR(a, 0, names.size()){
    aux = 9 - names[a].size();
    FOR(i, 0, v.size()){
      FOR(j, 0, aux+1){
        comp = v[i].substr(j, names[a].size());
        if(is_permutation(comp.begin(), comp.end(), names[a].begin()))
          veces[a]++;
      }
    }
  }
  
  FOR(i, 0, 8)
    cout << veces[i] << " ";*/
    
  cout << "KABIR\n";
  
  return 0;
}